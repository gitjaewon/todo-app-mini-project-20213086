# Required environment variables for this project
# - MONGODB_URI: required for local backend run and Vercel deployment
# - PORT: optional for local backend run only

MONGODB_URI=mongodb+srv://your-username:your-password@your-cluster-url/todoApp?retryWrites=true&w=majority
PORT=5000
